services:
  - type: web
    name: eirix
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: node dist/server/node-build.mjs
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
